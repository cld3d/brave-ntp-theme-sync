#!/bin/bash

# Apply MSI RGB pattern with the new theme colors.
# Copy this file to ~/.config/omarchy/hooks/theme-set to activate.
omarchy-msi-rgb-apply 2>/dev/null &

# Sync Omarchy wallpaper to Brave's New Tab Page background
~/.local/bin/omarchy-brave-wallpaper-sync 2>/dev/null &
